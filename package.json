{
  "name": "vendor-check-api",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "docker:build": "docker build -t vendor-check-api .",
    "docker:run": "docker run -p 8080:8080 vendor-check-api",
    "docker:test": "docker run --rm -p 8080:8080 vendor-check-api"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "playwright": "^1.40.0"
  }
}